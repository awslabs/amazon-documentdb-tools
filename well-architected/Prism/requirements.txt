# Core framework
dash==2.17.1
dash-bootstrap-components==1.6.0
plotly==5.17.0
# Data
pandas==2.0.3
numpy>=1.24.0,<2.0.0   # pandas 2.0.3 has C extensions built against numpy 1.x; numpy 2.x breaks import

# Database
pymongo==4.6.3   # >=4.6.3 fixes CVE-2024-5629 (bson out-of-bounds read); API-compatible with our usage

# Auth DB encryption (AES-256 at rest via SQLCipher)
sqlcipher3>=0.5.0

# AWS
boto3>=1.34.0
botocore>=1.34.0

# Compression analysis
lz4==4.3.2
zstandard==0.21.0

# PDF generation
fpdf2>=2.7.0
reportlab>=4.0.0

# Config
pyyaml>=6.0

# HTTP (pricing API)
requests>=2.31.0

# Production WSGI server (optional for local dev)
# gunicorn>=22.0.0
